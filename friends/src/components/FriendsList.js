import React, { useState } from 'react'
import {axiosWithAuth} from '../utils/axiosWithAuth';

const FriendsList = () => {
    return(
        <h2>Welcome to the Friends List</h2>
    )
}

export default FriendsList